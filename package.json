{
  "name": "eslintcc",
  "version": "0.0.0-pre.2",
  "description": "ESLintCC is a ECMAScript/JavaScript tool that computes cyclomatic complexity by using ESLint",
  "keywords": [
    "eslint",
    "javascript",
    "linter",
    "static-code-analysis",
    "cyclomatic-complexity"
  ],
  "repository": "github:eslintcc/eslintcc",
  "license": "MIT",
  "main": "./source/complexity.js",
  "bin": {
    "eslintcc": "eslintcc"
  },
  "files": [
    "source",
    "eslintcc"
  ],
  "dependencies": {
    "eslint": "^5.9.0",
    "@ndk/env": "^0.0.3"
  },
  "devDependencies": {
    "@ndk/test": "^0.0.4"
  },
  "scripts": {
    "test": "node test",
    "coverage": "nyc --reporter=text --all node test & nyc report --reporter=html",
    "test_with_send_a_coverage": "nyc --reporter=lcov --all node test & nyc report --reporter=text & coveralls < coverage/lcov.info"
  }
}